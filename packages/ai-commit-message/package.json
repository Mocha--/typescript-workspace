{
  "name": "@mooocha/ai-commit-message",
  "version": "1.0.7",
  "description": "Generate commit messages with AI from the current git diff and git branch name",
  "license": "ISC",
  "author": "Xibo Wang",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pnpm clean && tsc && cp src/prepare-commit-msg.template.txt dist/",
    "check-types": "tsc --noEmit",
    "prepublishOnly": "pnpm build"
  },
  "dependencies": {
    "@google/genai": "^1.8.0",
    "change-case": "^5.4.4",
    "commander": "^14.0.0"
  },
  "devDependencies": {
    "@mooocha/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:"
  },
  "bin": {
    "aimsg": "dist/cli.js"
  }
}
